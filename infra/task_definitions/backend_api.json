{
  "containerDefinitions": [
    {
      "name": "api",
      "image": "$IMAGE_URI",
      "cpu": 0,
      "portMappings": [
        {
          "containerPort": 3000,
          "hostPort": 3000,
          "protocol": "tcp"
        }
      ],
      "essential": true,
      "entryPoint": [],
      "command": [
        "rails",
        "s",
        "-b",
        "0.0.0.0"
      ],
      "environment": [
        {
          "name": "RAILS_ENV",
          "value": "production"
        }
      ],
      "secrets": [
        {
          "name": "DATABASE_URL",
          "valueFrom": "$SECRETS_MANAGER_ARN:DATABASE_URL::"
        }
      ],
      "mountPoints": [],
      "volumesFrom": [],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/venus-test",
          "awslogs-region": "ap-northeast-1",
          "awslogs-stream-prefix": "backend/api"
        }
      }
    }
  ],
  "family": "venus-test-backend-api-tmp",
  "taskRoleArn": "$TASK_ROLE_ARN",
  "executionRoleArn": "$TASK_EXECUTION_ROLE_ARN",
  "networkMode": "awsvpc",
  "volumes": [],
  "placementConstraints": [],
  "runtimePlatform": {
    "operatingSystemFamily": "LINUX"
  },
  "requiresCompatibilities": [
    "FARGATE"
  ],
  "cpu": "256",
  "memory": "512"
}
